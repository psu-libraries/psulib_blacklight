# frozen_string_literal: true

require 'rails_helper'

RSpec.describe 'Individual Record Publication Fields', type: :feature do
  describe 'Individual record publication fields', js: true do
    before do
      #visit individual record
    end

    context 'when the record has multiple indicators for MARC field 264' do
    end

    context 'when the record has MARC field 260 instead of 264' do
    end

    context 'when the record has limited publicaton data' do
    end

    context 'when the record has full publication data' do
    end 
  end
end
