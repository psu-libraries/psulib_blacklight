require 'rails_helper'

RSpec.feature "RecordPageFields", type: :feature do
  feature "Title fields" do

    scenario "User visits a document with a vernacular title" do

    end

    scenario "User visits a document without a vernacular title" do

    end

    scenario "User visits a document without a uniform title" do

    end

    scenario "User visits a document without additional titles" do

    end

    scenario "User visits a document without a series title" do

    end

  end

end
