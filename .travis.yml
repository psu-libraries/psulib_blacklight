language: ruby
rvm:
- 2.5.3

bundler_args: --without development
sudo: false

jdk:
- oraclejdk8

cache:
  directories:
  - tmp/solr_dl/

before_script:
- bundle exec rake assets:precompile

script:
- bundle exec niftany
- bundle exec rake

env:
- TZ=America/New_York
   global:
   - RAILS_ENV=test